fastapi
uvicorn[standard]

sqlalchemy>=2.0
psycopg2-binary

pydantic
pydantic-settings
python-dotenv

PyJWT

stripe>=8.0.0

# Pygments (transitive, ex. pytest): CVE-2026-4539 — quando existir 2.19.3+ no PyPI,
# adicionar pin explícito pygments>=2.19.3 e voltar a correr pip-audit.

# Testing
pytest
httpx