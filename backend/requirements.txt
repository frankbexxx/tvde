# MAL-2026-4750: evitar 0.136.3 (dep opcional fastar não documentada); pin até fix PyPI.
fastapi>=0.115,<0.136.3
uvicorn[standard]

sqlalchemy>=2.0
psycopg2-binary
alembic>=1.13

pydantic
pydantic-settings
python-dotenv
python-multipart

# IANA time zones for zoneinfo (Windows / minimal Python images).
tzdata

# PyJWT — PYSEC-2025-183 / CVE-2025-45768: aviso disputado (comprimento da chave HS256 é da app).
# pip-audit ignora em CI até existir fix no PyPI; JWT_SECRET_KEY >= 32 chars em prod/CI.
PyJWT>=2.12.0

bcrypt>=4.0.0

stripe>=8.0.0

# CVE-2026-25645 (pip-audit)
requests>=2.33.0

# Error tracking (opcional, só activa se SENTRY_DSN estiver definido).
sentry-sdk[fastapi]>=2.20.0

# Pygments (transitive, ex. pytest): CVE-2026-4539 — quando existir 2.19.3+ no PyPI,
# adicionar pin explícito pygments>=2.19.3 e voltar a correr pip-audit.

# Google OAuth (login social — troca code server-side)
google-auth>=2.40.0

# Testing
pytest
httpx

# Lint (local + npm run lint em backend/)
ruff>=0.9.0