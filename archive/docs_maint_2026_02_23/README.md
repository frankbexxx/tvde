# Arquivo de documentação — 2026-02-23

Ficheiros **movidos** da pasta ativa `docs/` (sem apagar). Motivo: prompts já implementadas, análises pontuais, imagens de sessão e notas auxiliares.

## Conteúdo

| Pasta | O quê |
|-------|--------|
| `prompts/` | Prompts A001–A013, B*, C*, D*, resumos de auditoria, e subpasta `archive_prompts/` (PROMPT_* antigos) |
| `assets/` | Imagens: `docs_image/` (ex-`docs/image`), `prompts_image/` (ex-`docs/prompts/image`) |
| `misc/` | Rascunhos, análises one-off, ficheiros `.txt`, merges duplicados |
| `vision/` | Documentos de estratégia / go-to-market (ex-`docs/vision/`) |

## O que ficou em `docs/` (ativo)

- `architecture/` — blueprint, status, convenções, roadmap (sem ficheiros duplicados)
- `testing/` — livros de teste
- `IMPLEMENTACAO_E_TESTES.md`, `ESTRUTURA_GUI.md`, `LOGS_E_TESTES_SINTESE.md`, `STACK_TECNOLOGICO.md`, `CRON_JOB_ORG_INSTRUCOES.md`, `DEBUG_BETA_RENDER.md`
- `prompts/A000_SYSTEM_RULES.md` — regras do projeto para agentes
- `prompts/A014_UX_POLISH.md` — trabalho em curso

Para recuperar algo: procura por nome nesta árvore ou no histórico Git (`git log --follow -- <path>`).
