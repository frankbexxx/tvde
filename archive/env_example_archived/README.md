# Arquivo: .env.example

Arquivado em 2025. O projeto usa `scripts/merge_env_keys.ps1` para adicionar chaves em falta ao `.env` com valores reais (não placeholders).

O ficheiro `.env.example` original estava aqui; foi removido de `backend/` por não ser utilizado.
